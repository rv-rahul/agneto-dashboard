{
  "name": "agneto-dashboard-workspace",
  "version": "1.0.0",
  "description": "Team Agneto Dashboard — monorepo workspace (backend + frontend)",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && node src/server.js",
    "start:frontend": "cd frontend && npx ng serve",
    "dev:backend": "cd backend && npx nodemon src/server.js",
    "dev:frontend": "cd frontend && npx ng serve",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
